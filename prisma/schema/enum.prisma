
enum Role {
     USER
     ADMIN
} 

enum UserStatus {
      ACTIVE
      INACTIVE
      BLOCKED
}

enum Gender {
     MALE
     FEMALE
}

enum FriendshipStatus {
    PENDING
    ACCEPTED
    BLOCKED
}